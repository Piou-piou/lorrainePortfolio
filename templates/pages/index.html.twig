{% extends 'base.html.twig' %}

{% block body %}
    <div class="ribs-container projects">
        <div class="row center-xs start-lg">
            {% for i in 1..7 %}
                <div class="cxs-8 cmd-6 clg-4 block">
                    <div class="bg-image" style="background-image: url({{ asset('images/accueil/0'~i~'.jpg') }})"></div>

                    <div class="bg-text">
                        <h3>Titre du projet</h3>
                        <div class="border"></div>
                        <p>Description du projet</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}